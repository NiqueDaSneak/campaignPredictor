{
    "name": "campaign-predictor",
    "version": "1.0.0",
    "description": "A pipeline to scrape and process data for campaign predictions",
    "main": "index.js",
    "scripts": {
        "start": "tsc && node dist/preprocessing/combine_data.js",
        "preprocess": "tsc && node dist/preprocessing/combine_data.js",
        "autotrain": "tsc && node dist/autotrain/upload_data.js"
    },
    "dependencies": {
        "axios": "^0.21.1",
        "csv-parser": "^3.0.0",
        "dotenv": "^10.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.14.12",
        "typescript": "^5.5.4"
    }
}
